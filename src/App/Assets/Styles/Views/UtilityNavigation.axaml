<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style Selector="Image.utility-nav">
        <Setter Property="Stretch" Value="Uniform"/>
    </Style>

    <!--
        utility-nav — square icon button in the right utility rail.

        Intentional differences from the global Button base:
        - Fixed 33×33 size
        - CornerRadius=CornerRadiusButtons (8) — rounder than control radius
        - Hover dims to Opacity=0.7 instead of showing an alpha overlay
        - Pressed triggers scale(0.85) micro-animation
        - Hover/pressed use BrushPrimaryBackgroundLighter (opaque surface lift)
    -->
    <Style Selector="Button.utility-nav">
        <Setter Property="Width"  Value="33"/>
        <Setter Property="Height" Value="33"/>
    </Style>

    <Style Selector="Button.utility-nav /template/ ContentPresenter">
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusButtons}"/>
    </Style>

    <Style Selector="Button.utility-nav:pointerover">
        <Setter Property="Opacity" Value="0.7"/>
    </Style>
    <Style Selector="Button.utility-nav:pointerover /template/ ContentPresenter">
        <Setter Property="Background" Value="{StaticResource BrushPrimaryBackgroundLighter}"/>
    </Style>

    <Style Selector="Button.utility-nav:pressed">
        <Setter Property="RenderTransform" Value="scale(0.85)"/>
    </Style>
    <Style Selector="Button.utility-nav:pressed /template/ ContentPresenter">
        <Setter Property="Background" Value="{StaticResource BrushPrimaryBackgroundLighter}"/>
    </Style>

    <!-- ── Notification count badge ──────────────────────────────────────── -->
    <Style Selector="Border.notif-badge">
        <Setter Property="Background"  Value="#3B82F6"/>
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusRound}"/>
        <Setter Property="Padding"     Value="3,0"/>
        <Setter Property="MinWidth"    Value="14"/>
        <Setter Property="Height"      Value="14"/>
    </Style>

    <Style Selector="TextBlock.notif-badge-text">
        <Setter Property="Foreground"          Value="White"/>
        <Setter Property="FontSize"            Value="8"/>
        <Setter Property="FontWeight"          Value="Bold"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment"   Value="Center"/>
        <Setter Property="TextAlignment"       Value="Center"/>
    </Style>

</Styles>
