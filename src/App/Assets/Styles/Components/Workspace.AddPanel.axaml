<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ── Layout ─────────────────────────────────────────────────────────── -->

    <Style Selector="Border.addpanel-main-border">
        <Setter Property="Background"      Value="{StaticResource BrushPrimaryBackground}"/>
        <Setter Property="BorderBrush"     Value="{StaticResource BrushBorder}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Padding"         Value="16"/>
        <Setter Property="BoxShadow"       Value="0 8 32 0 #55000000"/>
    </Style>

    <Style Selector="Grid.addpanel-main-grid">
        <Setter Property="RowSpacing" Value="16"/>
    </Style>

    <Style Selector="StackPanel.addpanel-body">
        <Setter Property="Spacing" Value="16"/>
    </Style>

    <Style Selector="StackPanel.addpanel-controls">
        <Setter Property="Spacing" Value="4"/>
    </Style>

    <Style Selector="Border.panel-divider">
        <Setter Property="Height"      Value="1"/>
        <Setter Property="CornerRadius" Value="0"/>
        <Setter Property="Background"  Value="{DynamicResource BrushBorder}"/>
        <Setter Property="Opacity"     Value="0.30"/>
    </Style>

    <!-- ── addpanel-icon — close / action button in panel/modal headers ───── -->
    <!--
        Intentional differences from the global Button base:
        - Fixed 28×28 with a 14×14 inner image
        - CornerRadius=3 (slightly tighter than CornerRadiusControl=4)
        Hover/pressed visuals come from the global base (BrushHoverOverlay /
        BrushPressedOverlay), which match this button's intended subtlety.
    -->
    <Style Selector="Button.addpanel-icon">
        <Setter Property="Width"  Value="28"/>
        <Setter Property="Height" Value="28"/>
        <Style Selector="^ /template/ ContentPresenter">
            <Setter Property="CornerRadius" Value="3"/>
        </Style>
        <Style Selector="^ Image">
            <Setter Property="Width"  Value="14"/>
            <Setter Property="Height" Value="14"/>
        </Style>
    </Style>

    <!-- ── Text ───────────────────────────────────────────────────────────── -->

    <Style Selector="TextBlock.form-section-header">
        <Setter Property="FontSize"   Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{DynamicResource BrushTextSecondary}"/>
        <Setter Property="Margin"     Value="0,0,0,12"/>
    </Style>

    <Style Selector="TextBlock.addpanel-text">
        <Setter Property="FontSize"            Value="{StaticResource FontSizeMedium}"/>
        <Setter Property="Foreground"          Value="{DynamicResource BrushTextFaded}"/>
        <Setter Property="TextWrapping"        Value="Wrap"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
    </Style>

    <!-- ── Form inputs ────────────────────────────────────────────────────── -->
    <!--
        addpanel-oneliner / addpanel-multiline extend the global TextBox base.
        Only size, layout, and multiline-specific properties are set here.
    -->

    <Style Selector="TextBox.addpanel-oneliner">
        <Setter Property="MinHeight"           Value="36"/>
        <Setter Property="MinWidth"            Value="300"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
    </Style>

    <Style Selector="TextBox.addpanel-multiline">
        <Setter Property="MinHeight"           Value="90"/>
        <Setter Property="MinWidth"            Value="300"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="TextWrapping"        Value="Wrap"/>
    </Style>

    <!--
        addpanel-combo extends the global ComboBox base.
        FontSize=13 and Padding are intentional variants (base uses 14 / 10,7).
    -->
    <Style Selector="ComboBox.addpanel-combo">
        <Setter Property="MinHeight"           Value="36"/>
        <Setter Property="MinWidth"            Value="180"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="FontSize"            Value="13"/>
        <Setter Property="Padding"             Value="14,6"/>
    </Style>

    <!-- No global NumericUpDown base — all properties explicit. -->
    <Style Selector="NumericUpDown.addpanel-num">
        <Setter Property="MinHeight"             Value="32"/>
        <Setter Property="MinWidth"              Value="180"/>
        <Setter Property="FontSize"              Value="13"/>
        <Setter Property="Padding"               Value="14,6"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="CornerRadius"          Value="{StaticResource CornerRadiusControl}"/>
        <Setter Property="BorderThickness"       Value="1"/>
        <Setter Property="BorderBrush"           Value="{DynamicResource BrushBorder}"/>
    </Style>

    <!-- No global TimePicker base — all properties explicit. -->
    <Style Selector="TimePicker.addpanel-time">
        <Setter Property="MinWidth"        Value="300"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="FontSize"        Value="13"/>
        <Setter Property="CornerRadius"    Value="{StaticResource CornerRadiusControl}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush"     Value="{DynamicResource BrushBorder}"/>
    </Style>

    <!-- No global DatePicker base — all properties explicit. -->
    <Style Selector="DatePicker.addpanel-date">
        <Setter Property="MinWidth"        Value="300"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="FontSize"        Value="13"/>
        <Setter Property="CornerRadius"    Value="{StaticResource CornerRadiusControl}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush"     Value="{DynamicResource BrushBorder}"/>
    </Style>

    <!-- ── Containers ─────────────────────────────────────────────────────── -->

    <Style Selector="Border.addpanel-task-template">
        <Setter Property="Background"      Value="#0AFFFFFF"/>
        <Setter Property="BorderBrush"     Value="{DynamicResource BrushBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius"    Value="{StaticResource CornerRadiusCard}"/>
        <Setter Property="Padding"         Value="14,12"/>
    </Style>

    <Style Selector="Expander.addpanel-expander">
        <Setter Property="MinWidth"              Value="180"/>
        <Setter Property="HorizontalAlignment"   Value="Stretch"/>
        <Setter Property="Background"            Value="{StaticResource BrushPrimaryBackgroundLight}"/>
        <Setter Property="FontSize"              Value="13"/>
        <Setter Property="FontWeight"            Value="Regular"/>
        <Setter Property="Padding"               Value="14,6"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="CornerRadius"          Value="{StaticResource CornerRadiusControl}"/>
        <Setter Property="BorderThickness"       Value="1"/>
        <Setter Property="BorderBrush"           Value="{DynamicResource BrushBorder}"/>
        <!-- Header surface — intentionally very dark -->
        <Style Selector="^ ToggleButton /template/ Border">
            <Setter Property="Background" Value="{StaticResource BrushBackgroundControl}"/>
        </Style>
        <!-- Checkbox inside an expander acts as a small chip: smaller + faded -->
        <Style Selector="^ CheckBox">
            <Setter Property="FontSize"   Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource BrushTextFaded}"/>
        </Style>
    </Style>

</Styles>
