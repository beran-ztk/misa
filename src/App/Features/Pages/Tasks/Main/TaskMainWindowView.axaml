<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="1500" d:DesignHeight="450"
             x:Class="Misa.Ui.Avalonia.Features.Pages.Tasks.Main.TaskMainWindowView"
             xmlns:main1="clr-namespace:Misa.Ui.Avalonia.Features.Pages.Tasks.Main"
             xmlns:header="clr-namespace:Misa.Ui.Avalonia.Features.Pages.Tasks.Header"
             xmlns:content="clr-namespace:Misa.Ui.Avalonia.Features.Pages.Tasks.Content"
             x:DataType="main1:TaskMainWindowViewModel">

    <!-- Außenfläche -->
    <Border Background="{StaticResource BrushPrimaryBackground}"
            Padding="12">

        <!-- 2 Zeilen: Header / Content -->
        <Grid RowDefinitions="Auto,*"
              RowSpacing="12">

            <!-- Header über volle Breite -->
            <Border Grid.Row="0"
                    Background="{StaticResource BrushPrimaryBackground}"
                    BorderBrush="{StaticResource BrushBorder}"
                    BorderThickness="0,1,0,1"
                    CornerRadius="12"
                    Padding="12">
                <header:TaskHeaderView DataContext="{Binding Header}" />
            </Border>

            <!-- Contentfläche: "Card"-Look -->
            <Border Grid.Row="1"
                    Background="{StaticResource BrushPrimaryBackgroundLight}"
                    CornerRadius="16"
                    Padding="12">

                <!-- 2 Spalten + dünner Splitter -->
                <Grid ColumnDefinitions="Auto,8,*"
                      ColumnSpacing="0">

                    <!-- Links: Liste-Container (greift optisch rein) -->
                    <Border Grid.Column="0"
                            Background="{StaticResource BrushPrimaryBackgroundLight}"
                            CornerRadius="14,0,0,14"
                            Padding="0"
                            Margin="0,0,-10,0"
                            BorderBrush="{StaticResource BrushBorder}"
                            BorderThickness="0">

                        <content:TaskContentView DataContext="{Binding Content}" />
                    </Border>

                    <!-- Splitter als "weichere" Zone -->
                    <GridSplitter Grid.Column="1"
                                  Width="8"
                                  Margin="20"
                                  CornerRadius="10"
                                  Background="{StaticResource BrushPrimaryBackgroundLighter}"
                                  HorizontalAlignment="Stretch" />

                    <!-- Rechts: Details-Container -->
                    <Border Grid.Column="2"
                            Background="{StaticResource BrushPrimaryBackgroundLight}"
                            ClipToBounds="True"
                            Padding="0"
                            Margin="12,0,0,0"
                            IsVisible="{Binding IsTaskSelected}">

                        <ContentControl Content="{Binding InfoView}" />
                    </Border>

                </Grid>
            </Border>

        </Grid>
    </Border>
</UserControl>
