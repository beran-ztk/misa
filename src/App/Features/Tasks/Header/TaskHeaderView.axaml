<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="Misa.Ui.Avalonia.Features.Tasks.Header.TaskHeaderView"
             xmlns:header="clr-namespace:Misa.Ui.Avalonia.Features.Tasks.Header"
             x:DataType="header:TaskHeaderViewModel">

    <UserControl.Styles>

        <!-- Add Button: pill + icon + text -->
        <Style Selector="Button.TaskAddButton">
            <Setter Property="Background" Value="{StaticResource BrushPrimaryBackgroundLighter}" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="999" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style Selector="Button.TaskAddButton:pointerover">
            <Setter Property="Background" Value="{StaticResource BrushPrimaryBackgroundLight}" />
        </Style>

        <!-- Search box: chip look -->
        <Style Selector="TextBox.TaskSearchBox">
            <Setter Property="Background" Value="{StaticResource BrushPrimaryBackgroundLight}" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="999" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Width" Value="240" />
        </Style>

        <Style Selector="TextBox.TaskSearchBox:pointerover">
            <Setter Property="Background" Value="{StaticResource BrushPrimaryBackgroundLighter}" />
        </Style>

        <Style Selector="TextBox.TaskSearchBox:focus">
            <Setter Property="Background" Value="{StaticResource BrushPrimaryBackgroundLighter}" />
        </Style>

    </UserControl.Styles>

    <Border Padding="12"
            Background="{StaticResource BrushPrimaryBackground}">

        <Grid ColumnDefinitions="Auto,*"
              ColumnSpacing="10"
              VerticalAlignment="Center">

            <!-- Add -->
            <Button Grid.Column="0"
                    Classes="TaskAddButton"
                    Command="{Binding ShowAddTaskWindowCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Image Width="16"
                           Height="16"
                           Source="{SvgImage /Assets/Images/MainNavigation/plus-circle.svg}" />
                    <TextBlock Text="Add"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource BrushTextPrimary}"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- Search -->
            <TextBox Grid.Column="1"
                     Classes="TaskSearchBox"
                     Watermark="Search tasks..."
                     Text="{Binding SearchText, Mode=TwoWay}"
                     HorizontalAlignment="Right" />
        </Grid>
    </Border>

</UserControl>
